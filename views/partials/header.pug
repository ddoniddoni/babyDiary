header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                i.fas.fa-camera-retro
        .header__column
            form(action=routes.search, method="get")
                input(type="text", placeholder="검색", name="term")        
        .header__column
            ul
                if !user
                    li
                        a(href=routes.join) 회원가입
                    li
                        a(href=routes.login) 로그인
                else 
                    li
                        a(href=`/videos${routes.uploadVideo}`) 동영상 등록
                    li
                        a(href=`/videos${routes.uploadImage}`) 사진 등록
                    li
                        a(href=routes.userDetail(user.id)) 프로필
                    li
                        a(href=routes.logout) 로그아웃  